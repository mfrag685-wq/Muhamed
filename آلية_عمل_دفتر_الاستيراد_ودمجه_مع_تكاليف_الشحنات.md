# آلية عمل «دفتر الاستيراد» — مرجع تقني لدمجه مع «تكاليف الشحنات»

> الغرض من هذا المستند: شرح **فكرة وآلية عمل دفتر الاستيراد** بتفصيل يكفي مطوّراً
> (أو مساعداً ذكياً) لِـ **نقل الفكرة إلى مشروع «دفتر تكاليف الشحنات»** ثم **دمج
> المشروعين في نظام واحد**. المستند مبني على الكود الفعلي لملف `index.html`
> (نفسه `دفتر_الاستيراد.html`).

---

## 1) ما الذي يفعله دفتر الاستيراد؟ (الفكرة في سطور)

دفتر الاستيراد يُدير **شحنات استيراد** ويحسب لكل قطعة **تكلفتها الواصلة الحقيقية**
(Landed Cost) = ثمن الشراء + نصيبها من المصاريف (شحن/جمارك/تخليص/نقل)، ثم يُخرج:

1. **تكلفة القطعة الواصلة** لكل موديل داخل كل حاوية.
2. **كشف حساب المورّد** (البضائع فقط) مدين/دائن ورصيد جارٍ.
3. **كشف حساب أجور الشحن** كحساب جارٍ منفصل (مدين/دائن) مع دفعات بنكية.
4. **مقارنة شركتَي استيراد** جنباً إلى جنب (نموذجان معزولان تماماً).
5. تقارير + استيراد/تصدير إكسل + نسخ احتياطي + مزامنة سحابية + صلاحيات مستخدمين.

**جوهر الفكرة القابل لإعادة الاستخدام** هو *محرّك توزيع التكاليف* (القسم 4)، وهو
بالضبط ما يحتاجه «تكاليف الشحنات». باقي الميزات (كشوف، مزامنة، صلاحيات) طبقات فوقه.

---

## 2) البنية التقنية العامة

| الجانب | التفاصيل |
|---|---|
| الواجهة | React 18 (مُصرَّفة إلى `React.createElement`، بدون JSX في المصدر) مضمّنة داخل ملف HTML واحد |
| المكتبات | React/ReactDOM + XLSX (SheetJS) + Firebase (compat) — كلها مضمّنة/مُحمّلة لتعمل دون إنترنت |
| التخزين | `localStorage` مع **بادئة لكل نموذج** لعزل البيانات: `p1::` أو `p2::` |
| العملات | تحويل ثنائي دولار↔ريال بسعر صرف قابل للتعديل، **ولكل مصروف عملته الخاصة** |
| السحابة | Firebase Realtime Database (اختياري) بمزامنة قائمة على رقم مراجعة `rev` |
| الأمان | دخول باسم/كلمة مرور (SHA‑256)، أدوار مدير/موظف، صلاحيات لكل تبويب |
| النشر | Cloudflare Pages (ملف `index.html` ثابت) |

بادئة النموذج النشِط تُقرأ من `window.__AP` (القيمة `"p1"` أو `"p2"`):

```js
const nsk = k => ((window.__AP) || "p1") + "::" + k;   // اسم المفتاح داخل localStorage
```

مفاتيح التخزين الأساسية (لكل نموذج): `ships-v4`, `pays-v3`, `cfg-v3` (يضم سعر
الصرف `rate`), `cfg-names` (أسماء الشركتين/النموذجين), `log-v1` (سجل النشاط).
مفاتيح الدخول عامة (غير مقيّدة بنموذج): `__auth_v1`, `__auth_session`.

---

## 3) نموذج البيانات (Data Model)

### 3.1 الشحنة (Shipment) — المفتاح `ships-v4`
الشحنة كائن هرمي: **شحنة ← حاويات ← أصناف (موديلات)**. المصاريف يمكن أن تكون على
مستوى الشحنة كلها *أو* على مستوى حاوية بعينها، ولكل مصروف عملته المستقلّة.

```jsonc
{
  "id": "abc123",
  "company": "c1",            // c1 أو c2 (شركة أولى/ثانية داخل نفس النموذج)
  "num": "SH-100",            // رقم الشحنة (مفتاح التجميع عند استيراد إكسل)
  "date": "2026-07-01",
  "currency": "USD",          // عملة الشحنة الأساسية: USD أو SAR
  "rate": 3.75,               // سعر صرف خاص بالشحنة (إن تُرك فارغاً يُستخدم العام)
  "note": "",
  "allocBasis": "value",      // أساس توزيع المصاريف: "value" (بالقيمة) أو "qty" (بالكمية)
  "extras":    { "freight": 0, "customs": 0, "clearance": 0, "transport": 0 },   // مصاريف على مستوى الشحنة
  "extrasCur": { "freight": "USD", "customs": "SAR", "clearance": "SAR", "transport": "SAR" }, // عملة كل مصروف
  "containers": [
    {
      "id": "c1",
      "number": "CONT-1",
      "extras":    { "freight": 0, "customs": 0, "clearance": 0, "transport": 0 }, // مصاريف خاصة بالحاوية
      "extrasCur": { "freight": "USD", "customs": "SAR", "clearance": "SAR", "transport": "SAR" },
      "items": [
        { "id": "i1", "model": "A-500", "qty": 100, "price": 12.5 }   // price = سعر شراء الوحدة (FOB)
      ]
    }
  ]
}
```

بنود المصاريف الأربعة ثابتة في الكود:

```js
const EXTRA_FIELDS = [
  ["freight",   "أجور الشحن"],
  ["customs",   "الجمارك"],
  ["clearance", "التخليص"],
  ["transport", "النقل"],
];
```

> `fixS(s)` هي دالة «التطبيع»: تضمن وجود كل الحقول بقيم افتراضية سليمة (تُصلح
> البيانات القديمة، تحوّل شكل `items` القديم إلى حاوية واحدة، وتضبط العملات).

### 3.2 الدفعة (Payment) — المفتاح `pays-v3`

```jsonc
{
  "id": "p1",
  "date": "2026-07-10",
  "amount": 5000,
  "currency": "SAR",          // USD أو SAR
  "rate": 3.75,
  "target": "both",           // both | c1 | c2  (لأي شركة تُحسب الدفعة)
  "kind": "goods",            // goods = دفعة بضاعة | freight = دفعة أجور شحن
  "method": "bank",           // bank | exchange (بنك/صرافة)
  "methodName": "الأهلي",
  "note": ""
}
```

**فكرة مهمة:** الحقل `kind` يفصل الدفعات إلى مسارين محاسبيين:
- `goods` → تدخل **كشف حساب المورّد (البضائع)**.
- `freight` → تدخل **كشف حساب أجور الشحن** (حساب جارٍ منفصل).

---

## 4) محرّك الحساب (قلب النظام)

كل الدوال تعمل على «عملة الشحنة» أولاً ثم تُحوَّل للريال عند العرض.

### 4.1 تحويل العملة
```js
const cvt = (amount, from, to, rate) => {
  const a = Number(amount) || 0;
  if (from === to) return a;
  if (from === "USD" && to === "SAR") return a * (rate || 3.75);
  if (from === "SAR" && to === "USD") return rate ? a / rate : 0;
  return a;
};
```

### 4.2 قيمة الشراء (FOB)
```js
const iC = it => qty(it) * price(it);            // FOB للموديل  = الكمية × سعر الوحدة
const cC = c  => Σ iC(items في الحاوية);          // FOB للحاوية
const sC = sh => Σ cC(كل الحاويات);               // FOB للشحنة  = قيمة الشراء
const sQ = sh => Σ الكميات;                        // إجمالي القطع في الشحنة
```

### 4.3 المصاريف (محوّلة لعملة الشحنة، كل مصروف بعملته)
```js
const exVal = (extras, extrasCur, shipCur, rate) =>
  Σ على البنود الأربعة: cvt(extras[k], extrasCur[k] || shipCur, shipCur, rate);

const shExtras = sh => exVal(مصاريف الشحنة) + Σ exVal(مصاريف كل حاوية);
const sLanded  = sh => sC(sh) + shExtras(sh);     // التكلفة الشاملة (Landed) للشحنة
```

### 4.4 ⭐ توزيع المصاريف على القطع (`landedMap`) — الفكرة الأساسية
هذه الدالة هي «الفكرة» التي يريد المستخدم نقلها. توزّع المصاريف الإضافية
**نِسبِيّاً** على القطع، إما حسب **القيمة** أو حسب **الكمية** (`allocBasis`):

- **مصاريف الحاوية** تُوزَّع على أصناف تلك الحاوية فقط، بحسب وزن كل صنف.
- **مصاريف الشحنة** تُوزَّع على **كل** أصناف الشحنة، بحسب وزن كل صنف.
- «الوزن» = القيمة (FOB للصنف) أو الكمية، حسب `allocBasis`.

```js
function landedMap(sh) {
  const basis = sh.allocBasis === "qty" ? "qty" : "value";
  const w = it => basis === "qty" ? qty(it) : iC(it);         // وزن الصنف في التوزيع
  const totW   = Σ w(كل أصناف الشحنة);
  const shipEx = exVal(مصاريف الشحنة);                        // بعملة الشحنة
  const map = {};
  for (const c of sh.containers) {
    const cEx = exVal(مصاريف الحاوية);
    const cW  = Σ w(أصناف هذه الحاوية);
    for (const it of c.items) {
      const fob   = iC(it);
      const extra = (cW ? cEx*w(it)/cW : 0)      // نصيبه من مصاريف الحاوية
                  + (totW ? shipEx*w(it)/totW : 0); // نصيبه من مصاريف الشحنة
      const landed = fob + extra;
      map[it.id] = { fob, extra, landed, unit: qty(it) ? landed/qty(it) : 0 };
    }
  }
  return map;   // unit = تكلفة القطعة الواصلة (المطلوب النهائي)
}
```

**مثال:** حاوية فيها صنفان: A (كمية 100، سعر 10 = FOB 1000) و B (كمية 100، سعر 30
= FOB 3000). مصاريف الشحنة 800. بالتوزيع **بالقيمة**: نصيب A = 800×1000/4000 = 200،
ونصيب B = 800×3000/4000 = 600. فتصبح تكلفة قطعة A الواصلة = (1000+200)/100 = **12**،
وقطعة B = (3000+600)/100 = **36**. بالتوزيع **بالكمية** يتساوى النصيبان (400 لكلٍّ).

### 4.5 التحويل للريال للعرض والكشوف
```js
const toSAR        = (a, cur, rate, def) => cur === "SAR" ? a : a * (rate || def || 3.75);
const shSAR        = (sh, def) => toSAR(sC(sh),      sh.currency, sh.rate, def); // FOB بالريال
const shLandedSAR  = (sh, def) => toSAR(sLanded(sh), sh.currency, sh.rate, def); // شامل بالريال
const pSAR         = (p,  def) => toSAR(p.amount,    p.currency,  p.rate,  def); // دفعة بالريال
```

### 4.6 كشف حساب المورّد (البضائع) — `ledger`
مدين = قيمة شراء الشحنات (FOB)، دائن = دفعات البضاعة (`kind ≠ freight`) المطابقة
للشركة، مع **رصيد جارٍ** مرتّب بالتاريخ:

```js
function ledger(ships, pays, co, def, from, to) {
  const SE = ships (لهذه الشركة، ضمن الفترة).map → { kind:"ship", dr: shSAR, cr:0 };
  const PE = pays  (kind!=="freight" && target∈{both,co}, ضمن الفترة).map → { kind:"pay", dr:0, cr: pSAR };
  rows = [...SE, ...PE] مرتّبة بالتاريخ؛  ثم رصيد جارٍ: run += dr - cr;
}
```

### 4.7 كشف حساب أجور الشحن (حساب جارٍ مستقل)
مبني كوحدة مستقلة (Vanilla JS، `window.__openFreight`)، **مدين** = أجور الشحن لكل
شحنة (محوّلة للريال)، **دائن** = الدفعات ذات `kind === "freight"`. يعرض في البيان
المبلغ **بالعملة الأصلية** بجانب رقم الشحنة (مثل `$15,000.00`)، وله زر طباعة/PDF.

### 4.8 الملخّص والمقارنة — `summ`
لكل شركة: عدد الشحنات/الحاويات/القطع، إجمالي FOB، إجمالي الشامل، المدفوع، الرصيد،
**متوسط تكلفة القطعة** ومتوسط تكلفة القطعة **الواصلة**. شاشة «المقارنة» تعرض
مخرجات `summ` للشركتين c1 و c2 جنباً إلى جنب.

---

## 5) التبويبات (الوظائف الظاهرة)

`ships` (الشحنات) · `pays` (الدفعات) · `reports` (التقارير) · `statement`
(كشف الحساب: بضائع + أجور شحن) · `compare` (المقارنة) · `log` (سجل النشاط).
الصلاحيات تُخفي التبويبات وأزرار الكتابة لغير المصرّح لهم.

**ميزات مساندة:** استيراد إكسل (يُجمِّع الصفوف حسب **رقم الشحنة**، لا حسب التاريخ)
· تصدير إكسل بنفس القالب · نسخة احتياطية JSON (تصدير/استيراد) · مزامنة سحابية
(اختيارية) · طباعة/PDF للكشوف · سِمة نهارية/ليلية · اسم وأيقونة قابلان للتعديل.

---

## 6) خطة دمج «دفتر الاستيراد» مع «تكاليف الشحنات»

المشروعان يتشاركان نفس الجوهر: **شحنة ← مصاريف ← توزيع → تكلفة القطعة**. الفرق أن
«تكاليف الشحنات» يركّز على حساب التكلفة، و«دفتر الاستيراد» يضيف فوقه المحاسبة
(كشوف/دفعات) والمقارنة بين شركتين. لذلك الدمج طبيعي.

### 6.1 المبدأ: نموذج بيانات موحّد + محرّك حساب مشترك
اجعل **الشحنة الهرمية** (القسم 3.1) هي نموذج البيانات المشترك، و**`landedMap`**
(القسم 4.4) هي دالة الحساب الوحيدة التي يعتمد عليها الطرفان. أي حقل يخصّ «تكاليف
الشحنات» فقط (مثل وزن/حجم الحاوية، رقم البوليصة، اسم الناقل) يُضاف داخل نفس الكائن
دون كسر الحساب.

### 6.2 خطوات عملية مقترحة
1. **وحّد بنود المصاريف**: تأكّد أن بنود «تكاليف الشحنات» تُطابق أو توسّع
   `EXTRA_FIELDS` (freight/customs/clearance/transport)، ويمكن إضافة بنود جديدة
   بمجرد التوسيع في مصفوفة واحدة — الحساب يمرّ على المصفوفة تلقائياً.
2. **انسخ محرّك الحساب** كما هو: `cvt`, `iC/cC/sC/sQ`, `exVal`, `shExtras`,
   `sLanded`, `landedMap`, `toSAR/shSAR/shLandedSAR`. هذه الدوال مستقلة عن الواجهة
   وقابلة للنقل مباشرة.
3. **أضف طبقة الدفعات والكشوف** (`fixP`, `ledger`, وحدة أجور الشحن) إن أردت أن
   يتحوّل «تكاليف الشحنات» إلى نظام محاسبي كامل مثل دفتر الاستيراد.
4. **وحّد التخزين**: استخدم نفس أسلوب البادئة `p1::/p2::` ومفاتيح `ships-v4`،
   `pays-v3`، `cfg-v3`، `cfg-names`. بذلك تصبح بيانات المشروعين متوافقة، ويمكن
   **استيراد نسخة أحدهما في الآخر** عبر نفس صيغة النسخ الاحتياطي JSON.
5. **دمج الواجهات**: أضف تبويبات دفتر الاستيراد (statement/compare/pays) إلى شريط
   تبويبات «تكاليف الشحنات»، أو العكس، مع الإبقاء على شاشة «الشحنات» موحّدة.
6. **مسار ترحيل البيانات**: اكتب دالة `migrate()` تقرأ مفاتيح «تكاليف الشحنات»
   القديمة وتحوّلها إلى شكل `ships-v4` عبر `fixS` (نفس فكرة الترقيات الموجودة:
   `ships-v3 → v4`).

### 6.3 أبسط طريق للدمج الفعلي
بما أن دفتر الاستيراد ملف واحد مكتفٍ ذاتياً، فالأسرع أن يصبح **دفتر الاستيراد هو
الأساس**، ويُضاف إليه ما ينقص من «تكاليف الشحنات» كحقول/شاشة، لأن دفتر الاستيراد
يحتوي أصلاً على: التوزيع النِّسبي، تعدّد العملات، الكشوف، الدفعات، المقارنة،
الإكسل، المزامنة، والصلاحيات — وهي مجموعة أشمل. عندئذ يكون «الدمج» = إضافة أي حقل
لوجستي ينفرد به «تكاليف الشحنات» إلى نموذج الشحنة، لا إعادة بناء.

---

## 7) ملخص المفاتيح والدوال (مرجع سريع)

| العنصر | المكان/الاسم | الوظيفة |
|---|---|---|
| بادئة النموذج | `window.__AP` + `nsk()` | عزل بيانات كل شركة/نموذج |
| مفاتيح التخزين | `ships-v4`,`pays-v3`,`cfg-v3`,`cfg-names`,`log-v1` | الشحنات/الدفعات/الإعدادات/الأسماء/السجل |
| تطبيع الشحنة | `fixS()` | يضمن سلامة الحقول ويُرقّي القديم |
| تطبيع الدفعة | `fixP()` | يضبط `kind`/`target`/العملة |
| FOB | `iC,cC,sC,sQ` | قيمة الشراء والكميات |
| المصاريف | `exVal,shExtras,sLanded` | مجموع المصاريف والتكلفة الشاملة |
| ⭐ التوزيع | `landedMap()` | تكلفة القطعة الواصلة (القلب) |
| التحويل للريال | `toSAR,shSAR,shLandedSAR,pSAR` | للعرض والكشوف |
| كشف البضائع | `ledger()` | مدين/دائن/رصيد جارٍ للمورّد |
| كشف أجور الشحن | `window.__openFreight` | حساب جارٍ مستقل + دفعات |
| الملخّص | `summ()` | إجماليات ومتوسطات لكل شركة |

---

*أُعدّ هذا المرجع من الكود الفعلي لدفتر الاستيراد ليكون أساساً لدمجه مع دفتر تكاليف الشحنات.*
