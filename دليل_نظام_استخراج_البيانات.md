# نظام استخراج وتنسيق بيانات الإكسيل والتقارير

نظام يعمل بالكامل داخل المتصفح (ملف واحد `نظام_استخراج_وتنسيق_بيانات_الاكسيل.html`) يأخذ **أي ملف إكسيل غير منسّق** ويحوّله إلى بيانات مرتّبة وتقارير احترافية جاهزة للتصدير والطباعة.

## الفكرة
ملفات التصدير الخام (مثل تصدير برامج المحاسبة) تحتوي على فوضى: صفوف فارغة، عناوين أشهر مثل `2026-06`، صفوف مجاميع (`المجموع` و`الإجمالي الكلي`) مبعثرة بين البيانات. النظام:
1. **يكتشف صف العناوين** تلقائياً.
2. **يطابق الأعمدة** (التاريخ، الفاتورة، العميل، المنتج، الكمية، السعر، الخصم، الضريبة، الإجمالي…) عبر كلمات مفتاحية — مع إمكانية التعديل اليدوي.
3. **ينظّف الصفوف**: يحذف الفارغ وعناوين الأشهر وصفوف المجاميع، ويصحّح الأرقام (عربية/إنجليزية، فواصل الآلاف، السالب بين قوسين، رموز العملة).
4. **يوحّد التواريخ** ويستخرج الشهر `YYYY-MM` لكل حركة.

## الاستخدام
1. افتح الملف `نظام_استخراج_وتنسيق_بيانات_الاكسيل.html` في المتصفح (يتطلب اتصال إنترنت لتحميل مكتبة القراءة).
2. اسحب ملفاً واحداً أو أكثر (`xls` / `xlsx` / `xlsm` / `csv`) — تُدمج كلها في مجموعة واحدة.
3. راجع مطابقة الأعمدة وعدّلها إن لزم ثم «إعادة المعالجة».
4. تنقّل بين التقارير، فلتر حسب الشهر، ابحث، افرز بالنقر على العناوين.
5. صدّر التقرير الحالي أو **كل التقارير** إلى Excel، أو اطبع (PDF).

## التقارير المتوفّرة
- **البيانات المنظّفة**: الجدول الكامل بعد التنظيف.
- **التقرير الشهري**: عدد الفواتير والكميات والخصومات والضرائب وصافي المبيعات لكل شهر.
- **أرصدة العملاء**: صافي رصيد كل عميل (الفواتير − المرتجعات) وعدد فواتيره.
- **تقرير المنتجات**: الكمية المباعة وإجمالي المبيعات ومتوسط السعر لكل منتج.
- **تقرير المندوبين**: أداء كل بائع/مندوب.
- **تقرير الفواتير**: ملخص كل فاتورة.

## ملاحظات
- كل المعالجة محلية داخل المتصفح؛ لا تُرفع بياناتك لأي خادم.
- النظام عام: يتكيّف مع أي ترتيب أعمدة بفضل الاكتشاف التلقائي والمطابقة اليدوية.

إعداد وتصميم: أ/ محمد فرج © 2026
