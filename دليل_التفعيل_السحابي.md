# دليل تفعيل المزامنة السحابية — نظام توريد الخضار والفواكه

الهدف: تدخل على البرنامج من **أي جهاز** (جوال أو كمبيوتر) فتجد بياناتك محفوظة، وكذلك لبقية المستخدمين — الكل يشترك في نفس البيانات وتتزامن فوراً.

التقنية المستخدمة: **Firebase Realtime Database** من Google (طبقة مجانية تكفي منشأة صغيرة/متوسطة).

> ما دامت إعدادات `CLOUD_CONFIG` فارغة، يعمل البرنامج محلياً على الجهاز كما كان تماماً. المزامنة تبدأ فور لصق إعداداتك.

---

## الخطوة 1 — إنشاء مشروع Firebase مجاني
1. افتح <https://console.firebase.google.com> وسجّل الدخول بحساب Google.
2. اضغط **Add project** (إضافة مشروع)، اكتب اسماً (مثل `veg-erp`)، ثم **Continue**.
3. يمكنك إيقاف Google Analytics (غير مطلوب)، ثم **Create project**.

## الخطوة 2 — تفعيل قاعدة البيانات
1. من القائمة الجانبية: **Build ← Realtime Database**.
2. اضغط **Create Database**، اختر المنطقة الأقرب (مثل `europe-west1`)، ثم **Start in locked mode** ← **Enable**.
3. انسخ **رابط قاعدة البيانات** الظاهر أعلى الصفحة (يبدأ بـ `https://...firebasedatabase.app`) — ستحتاجه لاحقاً في خانة `databaseURL`.

## الخطوة 3 — لصق قواعد الأمان
1. داخل Realtime Database افتح تبويب **Rules**.
2. استبدل المحتوى بما في ملف [`database.rules.json`](./database.rules.json) الموجود بجانب هذا الدليل، ثم **Publish**.
   - هذه القواعد تسمح بالقراءة والكتابة لمن اجتاز الدخول عبر Firebase فقط.

## الخطوة 4 — تفعيل الدخول المجهول (Anonymous)
1. من القائمة: **Build ← Authentication ← Get started**.
2. تبويب **Sign-in method** ← اختر **Anonymous** ← **Enable** ← **Save**.
   - هذا يسمح للبرنامج بالاتصال بالسحابة بأمان دون أن يطلب من الموظفين حساب Google. تبقى حماية الدخول عبر **اسم المستخدم وكلمة المرور** داخل البرنامج كما هي.

## الخطوة 5 — نسخ إعدادات المشروع
1. اضغط أيقونة الترس ⚙ أعلى اليسار ← **Project settings**.
2. انزل إلى قسم **Your apps** واضغط أيقونة الويب **`</>`**.
3. اكتب اسماً (مثل `web`) واضغط **Register app**.
4. ستظهر كتلة `firebaseConfig` — انسخ قيمها.

## الخطوة 6 — لصق الإعدادات في البرنامج
افتح ملف `نظام_توريد_الخضار_والفواكه_v3.html` بأي محرّر نصوص، ابحث عن `CLOUD_CONFIG` واملأ القيم:

```js
var CLOUD_CONFIG={
  apiKey:"AIza...",
  authDomain:"veg-erp.firebaseapp.com",
  databaseURL:"https://veg-erp-default-rtdb.firebasedatabase.app",  // من الخطوة 2
  projectId:"veg-erp",
  storageBucket:"veg-erp.appspot.com",
  messagingSenderId:"1234567890",
  appId:"1:1234:web:abcd...",
  workspaceId:"main"
};
```

> **مهم:** خانة `databaseURL` هي رابط الخطوة 2. إن نقصت لن تعمل المزامنة.

## الخطوة 7 — النشر على رابط ثابت
أسهل طريقة (بدون أدوات): **Netlify Drop**
1. أعد تسمية الملف إلى `index.html`.
2. افتح <https://app.netlify.com/drop> واسحب الملف إلى الصفحة.
3. ستحصل على رابط ثابت — وزّعه على المستخدمين ليفتحوه من أي جهاز.

بديل: **Firebase Hosting** (من نفس المشروع) عبر أداة `firebase-tools` إن كنت تفضّلها.

## الخطوة 8 — أول تشغيل والتحقق
1. افتح الرابط، سجّل الدخول (أول مرة: `admin` / `admin123`) — **غيّر كلمة مرور المدير فوراً** من تبويب «المستخدمون».
2. راقب المؤشّر أعلى الصفحة: **«متصل ☁»** يعني أن المزامنة تعمل.
3. جرّب إضافة صنف أو فاتورة، ثم افتح الرابط من جهاز آخر — ستجد نفس البيانات.

---

## كيف تعمل المزامنة (باختصار)
- **أول جهاز** تُفعّل عليه المزامنة يرفع بياناته الحالية إلى السحابة (تعبئة أولية).
- **بقية الأجهزة** تتبنّى بيانات السحابة تلقائياً عند فتحها.
- أي تعديل من أي مستخدم يظهر فوراً لدى الباقين (تحديث لحظي).

## تنبيهات مهمة
1. **ابدأ التفعيل من الجهاز الذي يحوي بياناتك الصحيحة.** الأجهزة الأخرى ستُستبدل بياناتها المحلية ببيانات السحابة — لذا صدّر «نسخة احتياطية» من أي جهاز قبل تفعيله للأمان.
2. **تجنّب أن يعدّل شخصان نفس السجل في نفس اللحظة تماماً**؛ فالنظام يحفظ آخر كتابة. التحديث اللحظي يقلّل هذا الاحتمال كثيراً لأن الجميع يرى تعديلات بعضهم فوراً.
3. مؤشّر الحالة: **«يُزامن…»** أثناء الحفظ، **«متصل ☁»** بعد نجاحه، **«تعذّر الاتصال»** عند انقطاع الإنترنت (تُحفظ البيانات محلياً وتُرفع عند عودة الاتصال).
4. النسخة الاحتياطية (JSON) تبقى مفيدة كطبقة أمان إضافية — نزّلها دورياً.

## رفع مستوى الأمان لاحقاً (اختياري)
النموذج الحالي يعتمد الدخول المجهول لـ Firebase + حماية اسم المستخدم/كلمة المرور داخل البرنامج. لمنشأة تريد حماية أقوى (بحيث لا تُقرأ قاعدة البيانات إلا لحسابات معتمدة على مستوى Firebase نفسه)، يمكن الترقية إلى **دخول Firebase بالبريد/كلمة المرور لكل مستخدم** وقواعد أمان تُقيّد الوصول بمعرّفات محددة. أخبرني إن رغبت بذلك.
