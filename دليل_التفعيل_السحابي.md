# دليل تفعيل المزامنة السحابية — نظام توريد الخضار والفواكه

الهدف: تدخل على البرنامج من **أي جهاز** (جوال أو كمبيوتر) فتجد بياناتك محفوظة، وكذلك لبقية المستخدمين — الكل يشترك في نفس البيانات وتتزامن فوراً، مع حماية قوية.

التقنية: **Firebase Realtime Database** من Google (طبقة مجانية تكفي منشأة صغيرة/متوسطة).

## وضعان للأمان (عبر `CLOUD_AUTH_MODE` داخل الملف)
- **`"accounts"` (الموصى به، وهو الافتراضي):** حساب Firebase مستقل لكل مستخدم (اسم مستخدم + كلمة مرور)، مع **قائمة عضوية** تمنع أي حساب غير معتمد من المدير من قراءة البيانات أو الكتابة. هذا الدليل يشرح هذا الوضع.
- `"anonymous"`: مساحة مشتركة بسيطة بدخول مجهول (حماية أخف) — لمن يريد أبسط إعداد. يستخدم ملف القواعد `database.rules.json` بدلاً من ملف الحسابات.

> ما دامت حقول `CLOUD_CONFIG` فارغة، يعمل البرنامج محلياً كما كان تماماً. المزامنة تبدأ فور لصق إعداداتك.

---

## الخطوة 1 — إنشاء مشروع Firebase مجاني
1. افتح <https://console.firebase.google.com> وسجّل الدخول بحساب Google.
2. **Add project** ← اكتب اسماً (مثل `veg-erp`) ← يمكنك إيقاف Analytics ← **Create project**.

## الخطوة 2 — تفعيل قاعدة البيانات
1. **Build ← Realtime Database ← Create Database** ← اختر المنطقة الأقرب ← **Start in locked mode** ← **Enable**.
2. انسخ **رابط قاعدة البيانات** أعلى الصفحة (يبدأ بـ `https://...firebasedatabase.app`) — لخانة `databaseURL`.

## الخطوة 3 — تفعيل الدخول بالبريد وكلمة المرور
1. **Build ← Authentication ← Get started**.
2. تبويب **Sign-in method** ← فعّل **Email/Password** ← **Enable** ← **Save**.
3. **مهم:** تأكّد أن **Anonymous معطّل** في هذا الوضع (كي لا يدخل أحد بلا حساب).

## الخطوة 4 — لصق قواعد الأمان (وضع الحسابات)
1. Realtime Database ← تبويب **Rules**.
2. استبدل المحتوى بما في ملف [`database.rules.accounts.json`](./database.rules.accounts.json) ← **Publish**.
   - هذه القواعد تسمح بقراءة/كتابة بيانات المنشأة **فقط لمن أضافه المدير كعضو**. أي حساب آخر — حتى لو سجّل في Firebase — لن يستطيع الوصول للبيانات.

## الخطوة 5 — نسخ إعدادات المشروع
1. الترس ⚙ ← **Project settings** ← قسم **Your apps** ← أيقونة الويب **`</>`**.
2. **Register app** ← انسخ قيم `firebaseConfig`.

## الخطوة 6 — لصق الإعدادات في البرنامج
افتح ملف `نظام_توريد_الخضار_والفواكه_v3.html`، وابحث عن `CLOUD_CONFIG` واملأ القيم:

```js
var CLOUD_CONFIG={
  apiKey:"AIza...",
  authDomain:"veg-erp.firebaseapp.com",
  databaseURL:"https://veg-erp-default-rtdb.firebasedatabase.app",  // من الخطوة 2
  projectId:"veg-erp",
  storageBucket:"veg-erp.appspot.com",
  messagingSenderId:"1234567890",
  appId:"1:1234:web:abcd...",
  workspaceId:"main"
};
var CLOUD_AUTH_MODE="accounts";   // حساب لكل مستخدم (لا تغيّرها إلا إذا أردت الوضع البسيط)
```

> **مهم:** خانة `databaseURL` هي رابط الخطوة 2. إن نقصت لن تعمل المزامنة.

## الخطوة 7 — النشر على رابط ثابت
أسهل طريقة: **Netlify Drop**
1. أعد تسمية الملف إلى `index.html`.
2. افتح <https://app.netlify.com/drop> واسحب الملف — ستحصل على رابط ثابت وزّعه على المستخدمين.

بديل: **Firebase Hosting** من نفس المشروع.

## الخطوة 8 — أول دخول (تجهيز حساب المدير)
1. افتح الرابط، وسجّل الدخول أول مرة بـ **`admin` / `admin123`**.
   - يُنشئ البرنامج **حساب المدير السحابي** تلقائياً ويسجّله كأول عضو معتمد.
2. اذهب فوراً إلى تبويب **المستخدمون** و**غيّر كلمة مرور المدير** إلى كلمة قوية (٦ خانات على الأقل).
3. تأكّد من ظهور المؤشّر **«متصل ☁»** أعلى الصفحة.

## الخطوة 9 — تفعيل حسابات بقية الموظفين
لكل موظف تريد أن يدخل من جهازه:
- أضِفه من تبويب **المستخدمون** (اسم، اسم مستخدم، كلمة مرور، دور) — يُنشأ حسابه السحابي **تلقائياً** عند الحفظ، أو
- اضغط زر **«حساب سحابي»** أمام اسمه في الجدول لتعيين كلمة مروره السحابية (يظهر **✓** بعد التفعيل).

بعدها يدخل الموظف من أي جهاز على نفس الرابط باسم المستخدم وكلمة المرور التي حدّدتها له، فيجد بيانات المنشأة كاملة حسب صلاحياته.

---

## كيف يعمل الأمان (باختصار)
- كل مستخدم له **حساب Firebase مستقل**؛ الدخول يتحقق منه على مستوى Google لا داخل الصفحة فقط.
- **قائمة العضوية** في قواعد الأمان تمنع أي حساب لم يعتمده المدير من رؤية البيانات أو تعديلها — حتى لو حصل على إعدادات المشروع.
- تبقى **الأدوار والصلاحيات** داخل البرنامج كما هي (مدير/محاسب/مدخل بيانات…).

## تنبيهات مهمة
1. **أكمِل تجهيز المدير فوراً بعد نشر الرابط** (الخطوة 8) — أول حساب يُسجَّل يصبح المدير المعتمد؛ إكماله بسرعة يغلق الباب أمام أي تسجيل غير مرغوب.
2. **ابدأ التفعيل من الجهاز الذي يحوي بياناتك الصحيحة**؛ الأجهزة الأخرى تتبنّى بيانات السحابة. صدّر «نسخة احتياطية» قبل تفعيل أي جهاز آخر للأمان.
3. **كلمة المرور المنسية:** لا يمكن للمدير رؤية كلمات المرور. لإعادة تعيينها، اضغط **«حساب سحابي»** أمام المستخدم وحدّد كلمة جديدة. إن ظهرت رسالة «حساب بكلمة مرور مختلفة»، احذف المستخدم من **Firebase ← Authentication ← Users** ثم أعد الضغط على «حساب سحابي».
4. **تجنّب أن يعدّل شخصان نفس السجل في نفس اللحظة تماماً**؛ يُحفظ آخر تعديل. التحديث اللحظي يقلّل هذا كثيراً لأن الجميع يرى تعديلات بعضهم فوراً.
5. مؤشّر الحالة: **«بانتظار الدخول»** قبل تسجيل الدخول، **«يُزامن…»** أثناء الحفظ، **«متصل ☁»** بعد نجاحه، **«تعذّر الاتصال»** عند انقطاع الإنترنت.
6. نزّل **نسخة احتياطية (JSON)** دورياً كطبقة أمان إضافية.

## استرجاع الوصول عند الطوارئ
إن تعذّر دخول أي مدير:
- من **Firebase ← Authentication** يمكنك إضافة/تعديل مستخدم يدوياً.
- أو مؤقتاً بدّل قواعد الأمان إلى محتوى `database.rules.json` (وصول لأي مستخدم مُوثّق) لحين إصلاح العضوية، ثم أعِد قواعد الحسابات.
