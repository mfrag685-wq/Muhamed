# دليل تفعيل النسخة السحابية — دفتر الاستيراد (تعمل على الجوال)

الهدف: تفتح **دفتر الاستيراد** من **أي جهاز** (جوال أو كمبيوتر) فتجد بياناتك محفوظة ومتزامنة. وكل **نموذج** (نموذج ١ / نموذج ٢) يُخزَّن في **مساحة سحابية منفصلة** عن الآخر تماماً.

التقنية: **Firebase Realtime Database** من Google — نفس طريقة مشروع التوريد. الطبقة المجانية (Spark: ‏1GB تخزين و10GB تنزيل شهرياً) **تكفي دفتر استيراد بمراحل، دون أي رسوم**.

> ما دامت إعدادات `CLOUD_CONFIG` فارغة، يعمل الملف محلياً/دون إنترنت تماماً كما هو الآن. المزامنة تبدأ فور لصق إعداداتك.

---

## الخطوة 1 — إنشاء مشروع Firebase مجاني
1. افتح <https://console.firebase.google.com> وسجّل الدخول بحساب Google.
2. **Add project** ← اكتب اسماً (مثل `import-ledger`) ← **Continue**.
3. أوقف Google Analytics (غير مطلوب) ← **Create project**.

## الخطوة 2 — تفعيل قاعدة البيانات
1. من القائمة الجانبية: **Build ← Realtime Database**.
2. **Create Database** ← اختر المنطقة الأقرب (مثل `europe-west1`) ← **Start in locked mode** ← **Enable**.
3. انسخ **رابط قاعدة البيانات** أعلى الصفحة (يبدأ بـ `https://...firebasedatabase.app`) — ستحتاجه في خانة `databaseURL`.

## الخطوة 3 — لصق قواعد الأمان
داخل Realtime Database افتح تبويب **Rules**، استبدل المحتوى بالتالي ثم **Publish**:

```json
{
  "rules": {
    "import-ledger": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
```

> تسمح بالقراءة والكتابة لمن اجتاز الدخول عبر Firebase فقط.

## الخطوة 4 — تفعيل الدخول المجهول (Anonymous)
1. **Build ← Authentication ← Get started**.
2. تبويب **Sign-in method** ← **Anonymous** ← **Enable** ← **Save**.
   - يسمح للبرنامج بالاتصال بأمان دون أن يطلب حساب Google من المستخدمين.

## الخطوة 5 — نسخ إعدادات المشروع
1. أيقونة الترس ⚙ أعلى اليسار ← **Project settings**.
2. قسم **Your apps** ← أيقونة الويب **`</>`** ← اكتب اسماً (مثل `web`) ← **Register app**.
3. ستظهر كتلة `firebaseConfig` — انسخ قيمها.

## الخطوة 6 — لصق الإعدادات في الملف
افتح `دفتر_الاستيراد.html` بأي محرّر نصوص، ابحث عن `CLOUD_CONFIG` (قرب نهاية الملف) واملأ القيم:

```js
var CLOUD_CONFIG={
  apiKey:"AIza...",
  authDomain:"import-ledger.firebaseapp.com",
  databaseURL:"https://import-ledger-default-rtdb.firebasedatabase.app",  // من الخطوة 2
  projectId:"import-ledger",
  storageBucket:"import-ledger.appspot.com",
  messagingSenderId:"1234567890",
  appId:"1:1234:web:abcd...",
  workspaceId:"main"   // اتركها "main"
};
```

> **مهم:** خانتا `apiKey` و`databaseURL` ضروريتان — إن نقصتا لن تعمل المزامنة.

## الخطوة 7 — النشر على Cloudflare Pages (للجوال)

### الطريقة (أ) — الربط بمستودع GitHub (موصى بها: نشر تلقائي عند كل تحديث)
المستودع يحتوي أصلاً على ملف `index.html` جاهز، لذا لا حاجة لأي رفع يدوي.
1. افتح <https://dash.cloudflare.com> وسجّل الدخول (حساب مجاني).
2. **Workers & Pages** ← **Create application** ← تبويب **Pages** ← **Connect to Git**.
3. اربط حساب GitHub واختر المستودع **`mfrag685-wq/Muhamed`**.
4. في **Set up builds and deployments**:
   - **Production branch:** `claude/error-handling-development-gknef4` (الفرع الذي فيه أحدث نسخة).
   - **Framework preset:** None.
   - **Build command:** اتركه فارغاً.
   - **Build output directory:** `/` (شرطة مائلة واحدة = الجذر).
5. **Save and Deploy** ← ستحصل على رابط `https://<اسم-المشروع>.pages.dev`.
6. أي تعديل لاحق يُدفع للفرع يُنشر تلقائياً خلال ثوانٍ.

### الطريقة (ب) — رفع مباشر (بدون Git)
1. أعد تسمية `دفتر_الاستيراد.html` إلى `index.html` وضعه وحده داخل مجلد فارغ.
2. **Workers & Pages** ← **Create application** ← **Pages** ← **Upload assets**.
3. اكتب اسم المشروع ← اسحب **المجلد** ← **Deploy site**.
4. للتحديث لاحقاً: ادخل المشروع ← **Create deployment** ← ارفع `index.html` الجديد.

## الخطوة 7-ب — ⚠️ ربط النطاق بـ Firebase (خطوة ضرورية للمزامنة)
الدخول المجهول في Firebase يرفض أي نطاق غير مُصرَّح به — وهذا غالباً سبب فشل الاستضافة. بعد أن تحصل على رابط `‎.pages.dev`:
1. في Firebase: **Authentication ← Settings ← Authorized domains ← Add domain**.
2. أضِف نطاق موقعك بدون `https://` (مثل `import-ledger.pages.dev`)، ثم **Add**.
3. إن استخدمت نطاقاً مخصّصاً لاحقاً، أضِفه هنا أيضاً.

> بدون هذه الخطوة سيظهر المؤشّر **«تعذّر الاتصال»** رغم صحة الإعدادات.

بديل للنشر: **Firebase Hosting** من نفس المشروع (يضيف النطاق للمصرَّح به تلقائياً).

## الخطوة 8 — أول تشغيل والتحقق
1. افتح الرابط. راقب المؤشّر أعلى يسار الشاشة:
   - **«يتصل بالسحابة…»** ثم **«متصل ☁»** يعني أن المزامنة تعمل (ويظهر بجانبه اسم النموذج الحالي).
   - **«تعذّر الاتصال — محفوظ محلياً»** عند انقطاع الإنترنت (تُحفظ البيانات وتُرفع عند عودته).
2. أضف شحنة، ثم افتح الرابط من جهاز آخر على **نفس النموذج** — ستجد نفس البيانات.

---

## كيف تعمل المزامنة (باختصار)
- كل **نموذج** له مساحته السحابية المنفصلة: `import-ledger/main/p1` و`import-ledger/main/p2`.
- **أول جهاز** يُفعّل المزامنة على نموذجٍ ما يرفع بياناته الحالية لتلك المساحة.
- **بقية الأجهزة** تتبنّى بيانات السحابة تلقائياً عند فتح نفس النموذج.
- أي تعديل يظهر لدى الباقين خلال ثوانٍ (تحديث لحظي).
- **التبديل بين النموذجين داخل البرنامج** يزامن كلاً مع مساحته المنفصلة — لا تختلط بيانات الشركتين.

## تنبيهات مهمة
1. **ابدأ التفعيل من الجهاز الذي يحوي بياناتك الصحيحة** لكل نموذج. الأجهزة الأخرى ستُستبدل بياناتها المحلية ببيانات السحابة — لذا خُذ «نسخة احتياطية» (زر ⬇) قبل التفعيل للأمان.
2. تجنّب أن يعدّل شخصان **نفس السجل** في نفس اللحظة؛ النظام يحفظ آخر كتابة.
3. النسخة الاحتياطية (JSON) واستيراد/تصدير إكسل تبقى مفيدة كطبقة أمان إضافية.
4. الملف يبقى يعمل **دون إنترنت**؛ فإن كنت لا تريد السحابة، اترك `CLOUD_CONFIG` فارغة.

## رفع مستوى الأمان لاحقاً (اختياري)
النموذج الحالي: دخول Firebase مجهول + قواعد تقصر الوصول على المصادَق عليهم. لمنشأة تريد حماية أقوى (حسابات بريد/كلمة مرور لكل مستخدم وقواعد تُقيّد كل نموذج بمعرّفات محددة) أخبرني لأرقّيها.
