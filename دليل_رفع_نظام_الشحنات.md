# دليل رفع نظام تكلفة الشحنات والتحديث التلقائي

هدفك: رابط ثابت لنظام تكلفة الشحنات يفتح من الكمبيوتر والجوال، ويتحدّث **تلقائياً** مع كل تعديل.

الملف الجاهز للرفع: **`shipments.html`** (نسخة نظام تكلفة الشحنات، تعمل بالكامل، وتدعم المزامنة السحابية عند تعبئة مفاتيح Firebase).

---

## الطريقة الأولى (الأسرع فوراً) — Netlify Drop
تُعطيك رابطاً خلال ثوانٍ بدون أي إعداد:
1. أعد تسمية `shipments.html` إلى `index.html` على جهازك.
2. افتح <https://app.netlify.com/drop> واسحب الملف.
3. ستحصل على رابط مثل `https://اسم-عشوائي.netlify.app` — افتحه على الجوال والكمبيوتر.

> عيبها: التحديث ليس تلقائياً — عند أي تعديل تسحب الملف الجديد مرة أخرى.

---

## الطريقة الثانية (تحديث تلقائي لحظة بلحظة) — GitHub Pages
بعد ضبطها مرة واحدة، **كل رفعة إلى الفرع `main` تُحدّث الرابط خلال ~دقيقة تلقائياً** — كما تريد.

الإعداد جاهز بالفعل في المستودع (ملف `.github/workflows/deploy.yml`). يتبقّى تفعيل واحد لمرة واحدة:

1. افتح المستودع على GitHub ← **Settings** (الإعدادات).
2. من القائمة الجانبية اختر **Pages**.
3. تحت **Build and deployment ← Source** اختر **GitHub Actions**.
4. احفظ. انتظر ~دقيقة حتى ينتهي النشر (تراه في تبويب **Actions** بعلامة ✅).

بعدها الروابط تصبح:
- نظام تكلفة الشحنات: `https://mfrag685-wq.github.io/Muhamed/shipments.html`
- (نظام الخضار والفواكه يبقى على الجذر: `https://mfrag685-wq.github.io/Muhamed/`)

> من الآن فصاعداً: أي تعديل يُرفع إلى `main` يظهر على هذا الرابط تلقائياً بعد دقيقة (حدّث الصفحة على جوالك Ctrl+F5 أو اسحب للتحديث).

---

## المزامنة السحابية (بيانات على كل الأجهزة)
رفع الموقع = يجعل **البرنامج** متاحاً على رابط. أما مزامنة **بياناتك** بين الأجهزة فتحتاج Firebase:
- افتح دليل [`دليل_التفعيل_السحابي_للشحنات.md`](./دليل_التفعيل_السحابي_للشحنات.md) واملأ `CLOUD_CONFIG` داخل `shipments.html`.
- بعدها سجّل الدخول من أي جهاز بنفس الحساب فتجد بياناتك متزامنة.

## تثبيت على الجوال كتطبيق
افتح الرابط في متصفح الجوال ← قائمة المتصفح ← **إضافة إلى الشاشة الرئيسية**، فيفتح كتطبيق بملء الشاشة.
