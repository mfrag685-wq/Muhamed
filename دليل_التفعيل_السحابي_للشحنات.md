# دليل تفعيل المزامنة السحابية — نظام تكلفة الشحنات

الهدف: تدخل على البرنامج من **أي جهاز** (جوال أو كمبيوتر) فتجد بياناتك محفوظة ومتزامنة فوراً — الشحنات، الإعدادات، الشركات، والدفعات — كلها في السحابة، وكل حساب يرى بياناته فقط.

التقنية: **Firebase Realtime Database** من Google (طبقة مجانية تكفي منشأة صغيرة/متوسطة).

> ما دامت حقول `CLOUD_CONFIG` فارغة، يعمل البرنامج **محلياً بالكامل** كما هو الآن. المزامنة تبدأ فور لصق إعداداتك.

---

## الخطوة 1 — إنشاء مشروع Firebase مجاني
1. افتح <https://console.firebase.google.com> وسجّل الدخول بحساب Google.
2. **Add project** ← اكتب اسماً (مثل `shipments-cost`) ← يمكنك إيقاف Analytics ← **Create project**.

## الخطوة 2 — تفعيل قاعدة البيانات
1. **Build ← Realtime Database ← Create Database** ← اختر المنطقة الأقرب ← **Start in locked mode** ← **Enable**.
2. انسخ **رابط قاعدة البيانات** أعلى الصفحة (يبدأ بـ `https://...firebasedatabase.app`) — لخانة `databaseURL`.

## الخطوة 3 — تفعيل الدخول بالبريد وكلمة المرور
1. **Build ← Authentication ← Get started**.
2. تبويب **Sign-in method** ← فعّل **Email/Password** ← **Enable** ← **Save**.

## الخطوة 4 — لصق قواعد الأمان (وضع الأعضاء المعتمدين)
1. Realtime Database ← تبويب **Rules**.
2. استبدل المحتوى بما في ملف [`database.rules.accounts.json`](./database.rules.accounts.json) ← **Publish**.
   - هذه القواعد تطبّق **نفس آلية نظام التوريد**: لا أحد يقرأ أو يكتب بيانات المنشأة إلا من **اعتمده المدير** (مسجّل في `workspaces/main/members`).
   - أول مدير يُنشئ حسابه يُسجَّل تلقائياً كأول عضو، ثم كل موظف يضيفه المدير من شاشة «المستخدمون» يُعتمَد تلقائياً في السحابة.
   - إيقاف أو حذف موظف يسحب وصوله للبيانات فوراً.
   > الملف القديم `database.rules.shipments.json` لم يعد مستخدماً (كان يعتمد الحساب الشخصي فقط دون قائمة أعضاء).

## الخطوة 5 — نسخ إعدادات المشروع
1. الترس ⚙ ← **Project settings** ← قسم **Your apps** ← أيقونة الويب **`</>`**.
2. **Register app** ← انسخ قيم `firebaseConfig`.

## الخطوة 6 — لصق الإعدادات في البرنامج
افتح ملف `نظام_تكلفة_الشحنات.html`، وابحث عن `CLOUD_CONFIG` (قرب نهاية الملف) واملأ القيم:

```js
var CLOUD_CONFIG={
  apiKey:"AIza...",
  authDomain:"shipments-cost.firebaseapp.com",
  databaseURL:"https://shipments-cost-default-rtdb.firebasedatabase.app",  // من الخطوة 2
  projectId:"shipments-cost",
  storageBucket:"shipments-cost.appspot.com",
  messagingSenderId:"1234567890",
  appId:"1:1234:web:abcd...",
  workspaceId:"main"
};
```

> **مهم:** خانة `databaseURL` هي رابط الخطوة 2. إن نقصت لن تعمل المزامنة.

## الخطوة 7 — النشر على رابط ثابت (لاستخدامه على الجوال)
أسهل طريقة: **Netlify Drop**
1. أعد تسمية الملف إلى `index.html`.
2. افتح <https://app.netlify.com/drop> واسحب الملف — ستحصل على رابط ثابت.
3. افتح الرابط على جوالك، سجّل الدخول بنفس البريد وكلمة المرور — ستجد بياناتك.

بديل: أضِف الملف إلى **Firebase Hosting** أو أي استضافة ثابتة.

---

## كيف تعمل المزامنة داخل البرنامج
- بعد لصق `CLOUD_CONFIG`، يظهر زر **☁️ سحابي** في الأعلى، وتفتح نافذة الدخول.
- **حساب جديد**: أدخل بريداً وكلمة مرور (6 أحرف فأكثر) ← «حساب جديد».
- **دخول**: بنفس البيانات من أي جهاز.
- 🟢 نقطة خضراء = متزامن | 🟡 صفراء = يحفظ الآن | 🔴 حمراء = خطأ اتصال.
- كل تغيير (شحنة، دفعة، شركة، إعداد) يُرفع تلقائياً، ويظهر على أجهزتك الأخرى فوراً.

## ملاحظات
- عند أول تسجيل دخول على جهاز فيه بيانات محلية والسحابة فارغة: تُرفع بياناتك المحلية للسحابة.
- إذا كانت السحابة تحتوي بيانات: تُسحب إلى الجهاز (تُصبح السحابة هي المصدر). لذا سجّل الدخول أولاً على الجهاز الذي فيه بياناتك الأصلية.
- النسخ الاحتياطي المحلي وتنزيل «نسخة شاملة» يظل يعمل كطبقة أمان إضافية.
- الوضع الليلي/النهاري وتفضيلات النسخ التلقائي تبقى محلية لكل جهاز (لا تُزامن).
